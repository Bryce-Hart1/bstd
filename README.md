# Welcome to mylib

Mylib is a multi-language project that contains pieces of code I have made over the years in multiple languages that is
all available to the public. It is up to you, the installer to check if classes are unfinished as this repo is constantly being added to and all code pieces may not work as intended.

## Contents of bstd (cpp)

Below is a list of where functions/classes are located in bstd.

``` cpp
bstd{
    ds{ //data structures
        ts{ //threadsafe
            
        }
        tree
    }
    encode{
        huffman
    }
    store{
        cypher{
            AES-256
        }
        varyingInt
        vault
    }

}
```


## Contends of bstd (rust)

```cpp
    src{
       bits{
            bit_string (used in my CPU EM)
       } 
       spell{
            parser 
       }
       store{
            fixed_data_storage (retired)
       }
       simple_hash
    }


```

## State of C and ASM

I am currently trying to learn X86-64 Asm and I would not trust either of these libraries in the current state, 
rather they are there it show my progress in learning.